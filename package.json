{
  "devDependencies": {
    "npm-run-all": "4.1.5",
    "husky": "9.0.11"
  },
  "scripts": {
    "prepare": "husky",
    "format": "run-s format:*",
    "format:client": "cd apps/client && yarn format",
    "format:waitlist": "cd apps/wait-list && yarn format",
    "format:main": "cd services/main && make lint"
  }
}
