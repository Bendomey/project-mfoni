{
  "devDependencies": {
    "npm-run-all": "4.1.5"
  },
  "scripts": {
    "format": "run-s format:*",
    "format:frontend-client": "cd apps/client && yarn format",
    "format:frontend-waitlist": "cd apps/wait-list && yarn format",
    "format:frontend-admin": "cd apps/admin && yarn lint --fix",
    "format:services-main": "cd services/main && make lint",
    "format:services-search": "cd services/search && make lint"
  }
}
